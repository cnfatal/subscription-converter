server:
  listen: 127.0.0.1:9099

# Applied to every subscription. The default patch format is clash-rules.
# patches:
#   - source: ./rules/global.yaml
#   - source: ./rules/sing-box.json
#     format: sing-box
#   - source: ./rules/advanced.yaml
#     format: patch

subscriptions:
  - name: primary
    source: https://example.com/subscription?token=replace-me
    # patches:
    #   - source: ./rules/primary.yaml
  - name: backup
    source: ./backup-clash.yaml
    format: clash
